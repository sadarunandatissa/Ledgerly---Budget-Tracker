Put any images the project uses in this folder (logo, favicon, screenshots).

The interface is drawn entirely with CSS and Chart.js, so no image files are required
for the app to run. This file only exists so the folder is kept in the submission.
